# Phase 2 backend (assistant relay + legacy translate).
#
# PINNED ON PURPOSE. Render reinstalls these from scratch on every deploy, so an
# unpinned list means a future push — even one that only touches the frontend —
# can pull a new major of supabase/fastapi and break the AI with no related code
# change. These exact versions were booted and probed on Python 3.12.13
# (see PYTHON_VERSION in render.yaml; keep the two in step).
#
# To upgrade: bump here, recreate a venv, `pip install -r requirements.txt`, run
# `uvicorn app.main:app` and check /health plus a real assistant call.
fastapi==0.139.2
uvicorn[standard]==0.51.0
anthropic==0.117.0
httpx==0.28.1
supabase==2.31.0
python-dotenv==1.2.2
